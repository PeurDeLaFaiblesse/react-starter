{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "dev":  {
      "cache": false
    },
    "build:dev": {
      "dependsOn": ["^build:dev"],
      "outputs": ["build/**"]
    },
    "check-type": {}
  }
}